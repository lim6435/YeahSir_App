<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="default_font" parent="@android:style/TextAppearance">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/text_color</item>
    </style>

    <style name="large_font" parent="default_font">
        <item name="android:textSize">@dimen/large_font_size</item>
    </style>

    <style name="normal_font" parent="default_font">
        <item name="android:textSize">@dimen/normal_font_size</item>
    </style>

    <style name="small_font" parent="default_font">
        <item name="android:textSize">@dimen/small_font_size</item>
    </style>

    <style name="default_button">
        <item name="android:background">@drawable/custom_button</item>
        <item name="android:textColor">@color/bt_text_color</item>
    </style>

</resources>
